# frozen_string_literal: true

source "https://rubygems.org"
group :jekyll_plugins do
  # the default jekyll-hashtags gem doesn't support the latest jekyll versions
  # gem "jekyll-hashtags", github: "middlebear/jekyll-hashtags"
end
gemspec
